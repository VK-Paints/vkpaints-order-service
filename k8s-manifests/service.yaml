apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: vkpaints-dev-ns
spec:
  selector:
    app: order-service
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
